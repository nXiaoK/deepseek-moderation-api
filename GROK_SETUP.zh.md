# Grok 审核接入与撤回

Grok 功能在 `codex/grok-audit-provider` 分支开发。原 DeepSeek 功能保存在标签 `baseline-before-grok-20260912`：

| 项目 | 基线提交 |
| --- | --- |
| 独立审核系统 | `a62bbf2` |
| sub2api | `37e832258` |

本机配置、密码与数据库不放入 Git。开发数据库在变更前保存到 `backups/before-grok-20260912-162121.sql`（仅当前用户可读，未跟踪）。代码回滚与数据库恢复是不同操作；本功能只增加数据库字段，一般撤回代码无需恢复整库。

## 1. sub2api 侧准备

先部署包含 Grok 专用入口的 sub2api 功能分支。在现有账号管理中完成 Grok OAuth 授权，并验证该账号能正常调用所选文本模型。审核系统不接收 Grok 密码、SSO 或 refresh token；续期与账号调度全部由 sub2api 维护。

创建一个独立的普通服务用户（不要复用被审核业务请求的用户），为其创建专用 API Key，并绑定一个：

- 平台为 Grok 的私有/专属分组。
- 已开启模型允许列表，使用明确文本模型名，例如实际验证可用的 `grok-4.6`。不使用 `*`、`latest` 或图片模型。
- 已绑定有效的 Grok OAuth 账号，确认账号并发、额度与代理配置。

为服务 Key 配置合理的配额、速率上限和来源 IP 限制。再在 sub2api 服务器环境中配置以下绑定并重启：

```dotenv
# 示例 ID，需要替换为实际的 Key ID、分组 ID 和服务用户 ID。
GROK_AUDIT_SERVICE_BINDINGS=123:45:67
# 多组绑定可用逗号分隔，每个条目都是 key_id:group_id:user_id。
```

默认空配置不开放内部审核入口。绑定采用数据库中的可信身份，不接受用户在请求中自报；变更 Key 所属用户、分组、分组平台或开放状态后连接会失效。

绑定的服务 Key 仅能请求：

```text
GET  /v1/audit/grok/capabilities
POST /v1/audit/grok/chat/completions
```

这两个入口仍先执行原 API Key 鉴权、用户/分组状态和计费额度检查。普通聊天、Responses、媒体、Google 格式等入口对该服务 Key 拒绝。管理 API 继续要求管理员登录权限。

通过专用 POST 入口验证的请求带服务端上下文标记，不再进入内容审核或提示词审计，避免循环。普通业务请求的两套审计保持原样，无需修改其全局开关或分组范围。

请求约束：明确模型允许列表、system + user 两条文本、非流式、temperature=0、JSON object 输出、max_tokens 128～512、每条文本最多约 64000 字符（含输入包装）、总请求体最多 1 MiB。不接受 tools、搜索、图片、任意附加参数或查询串。

Grok 实际模型转发复用现有 `GrokTokenProvider` 和调度链。审核专用请求固定走 Grok Chat Completions，避免 Responses 缓存路由注入搜索工具声明。若特定账号/模型不支持 Chat JSON 输出，试跑会明确失败，不会静默改用另一模型或普通业务入口。

## 2. 审核系统侧配置

在审核系统 `.env` 中显式允许你的 sub2api 源地址，再重启审核系统：

```dotenv
# 服务根源地址，不带 /v1。必须精确匹配协议、主机、端口。
AUDIT_SUB2API_ORIGINS=https://sub2api.example.com
# 本机/内网测试也必须显式列出相应 http://host:port。
```

服务器管理员通过此环境变量决定允许持有凭证访问哪些部署地址，后台不能把密钥任意转发到其他目标。跨主机生产连接使用 HTTPS；只有可信内网开发场景使用 HTTP。连接禁止跟随重定向。

后台操作：

1. 进入“模型密钥”，选择 **Grok · sub2api 专用服务**，填写连接根地址和上一节创建的服务 Key。
2. 新建策略，例如 `abuse-grok-v1`，可从当前 DeepSeek 策略复制提示词。原提示词不自动改写。
3. 在“模型与判定”选择 **Grok（经 sub2api OAuth 账号）**，选择刚保存的连接密钥。
4. 点击“验证连接并获取允许模型”，确认防循环能力已启用。列表代表分组允许模型，真实账号权益仍需试跑。
5. 选择明确模型，运行草稿试跑，再发布。发布与回滚到 Grok 版本前都会执行连接自检。
6. 原 sub2api 业务分组如需采用 Grok 审核，把“自定义审核服务”的模型别名改为 `abuse-grok-v1`；审核服务外部路径仍是 `/v1/moderations`。

凭证绑定的供应商和地址不可就地更改；换地址需新建凭证，防止现有密钥被发送到不同目的地。密钥轮换仍可在同一连接内完成。连接地址、明确模型和连接修订保存在不可变策略版本中。

后台提供“打开 sub2api 账号管理”链接，登录和重新授权在 sub2api 内完成；这里没有新建一套 Grok 密码登录或验证码处理流程。

## 3. 用量、成本与缓存

- DeepSeek 保持现有价格、时段规则和预算行为。
- Grok 记录输入、缓存 token、输出和推理 token。根据 total_tokens 判断推理 token 是否已经包含在 completion_tokens，避免重复计数；不能解释的用量保持未知。
- 保存实际响应模型及模型响应 ID，便于与 sub2api 日志关联。
- Grok OAuth 套餐/网关配额由 sub2api 管理。独立审核系统不会把 DeepSeek 单价套到 Grok，也不以“有套餐”等同“每次成本为零”。首次 Grok 货币成本显示待核对，可根据账单手工核对。
- 若调用方开启了人民币日/月预算，又没有可计算的 Grok 货币价格，本次调用会在发给模型前被拒绝。建议 Grok 使用单独的调用方 Key，并在 sub2api 侧设置额度，独立系统继续以 RPM 控制频率。逐次网关收费回执及 xAI 实扣价目尚未实现。
- 缓存按供应商、连接修订、凭证、策略版本和完整输入隔离。每次 Grok 请求（包括缓存命中）先做连接能力检查；失去服务权限或模型权限时停止复用。后台试跑不使用结果缓存。
- sub2api 修改账号/渠道模型映射后，更新审核策略“连接修订”并发布，使旧结果缓存失效；避免用 `latest` 模型别名。

不自动切换 Grok 与 DeepSeek，不自动降低输出规则。两者评分尺度可能不同，先用现有 `evals/manual-v1` 样本对照评估，再分别调整阈值。

## 4. 撤回方法

先在后台把业务使用的审核策略切回原 DeepSeek 版本/别名，停用 Grok 策略和 Grok 连接密钥，再在 sub2api 撤销专用服务 Key。随后撤回 Grok 代码提交并重新构建、重启两个服务。撤销专用 Key 很重要：旧版 sub2api 尚无这把 Key 的专用端点限制。

已有功能提交可使用 `git revert <Grok功能提交>` 创建反向提交，保留历史；不要使用 `reset --hard` 抹去后续工作。也可从基线创建恢复分支：

```sh
git switch -c recovery/deepseek-only baseline-before-grok-20260912
```

切分支前先提交或保存其他未完成改动。Git 不会回滚 `.env` 和数据库；保留 Grok 新增字段不会影响旧版读取。恢复整库会丢掉备份之后的数据，因此通常只撤回代码与策略，不自动导入旧 SQL。

## 5. 验证范围

自动测试覆盖：服务身份绑定、私有分组与模型约束、普通请求不能伪造审核豁免、受限请求不递归、OAuth 调用复用及无搜索工具、供应商凭证隔离、连接失效后的缓存处理、Grok 推理 token、未知成本保护，以及 DeepSeek 原功能回归。

真实 Grok 授权和调用需要部署 sub2api 新入口、配置以上服务器绑定与服务凭证。测试使用受控 HTTP 替身；没有在开发过程中索取、复制或调用用户的 Grok OAuth 凭证。
