# 多模型审核与简化配置设计

状态：设计记录，实际实现和配置说明以 README.md 为准。

实施约束（用户确认）：仅修改本审核项目，不修改 sub2api；不考虑旧开发数据兼容，因此不执行下文原建议的数据迁移和 sub2api 联动改动。实现保留 sub2api 当前接口必需的 policy_version 字段，后台无版本管理。通道绑定与当前规则一起保存在策略 config JSONB 中并原子保存；尝试详情保存在根请求 metadata，独立费用记录按 request_id 关联。通道统计为进程启动以来累计，暂不提供通道 24 小时/P95 图表。

## 1. 产品目标

删除审核策略的草稿管理、发布、版本历史、版本比较和回滚。策略只保留一份当前配置，点击“保存并生效”后，新请求使用新配置。

一个审核策略可以配置多个模型通道，按照“优先级分层，同层按权重分流，失败自动切换”的规则调用。默认一次请求只取得一个有效审核结果；有效结果无论是否违规都直接返回。

对外仍通过 `/v1/moderations` 和策略别名调用，例如 `abuse-audit-v1`。别名中的 `v1` 是已有调用名称，不自动改名，以免影响接入方。

保留审核记录、管理员操作记录、账单与费用核对记录。这些用于查询请求和费用，不提供策略版本管理或恢复功能。

## 2. 配置结构

| 对象 | 内容 | 放置位置 |
| --- | --- | --- |
| 审核策略 | 名称、别名、启停、提示词、阈值、缓存时间、原文保存、保留天数 | 审核策略 |
| 模型通道 | 名称、供应商、模型名、连接密钥、超时、输出上限、并发上限、启停 | 审核模型 |
| 策略通道绑定 | 所选通道、优先级、权重、是否参与该策略 | 策略内“模型调度” |
| 调度参数 | 总调用时限、最多调用次数、同层分配算法 | 策略内“模型调度” |
| 连接密钥 | 供应商、Base URL、加密保存的 API Key | 审核模型内“连接密钥”子页 |

一个通道表示一个“连接 + 模型”组合。同模型配两个不同 API Key，可建立两个通道；DeepSeek 与 Grok 也可以混用。连接地址以密钥绑定的地址为准，避免重复填写造成不一致。

通道可被多个策略引用，优先级和权重属于各自策略。通道并发上限在所有引用策略之间共享。编辑共享通道时，页面显示受影响的策略列表。

策略统一提示词、阈值和输出契约。不同模型的评分尺度可能不同，上线分流前用同一批样本比较误报和漏报；第一期不增加每模型提示词和阈值，降低配置复杂度。

## 3. 调度规则

### 优先级与负载均衡

优先级范围建议为 1～100，数字越小越优先；同级权重建议为 1～1000，必须为正整数。停用使用独立开关，不使用权重 0。

| 示例通道 | 模型 | 优先级 | 权重 | 正常状态下的用途 |
| --- | --- | --- | --- | --- |
| DeepSeek A | deepseek-flash | 1 | 70 | 主用，约占主组请求的 70% |
| DeepSeek B | deepseek-flash | 1 | 30 | 主用，约占主组请求的 30% |
| Grok 备用 | 用户填写的实际可用 Grok 模型名 | 2 | 100 | 主组无可用通道时承接请求 |

第一期只实现加权随机，不增加算法下拉菜单。每次从当前可用的最高优先级组中按权重抽取一个通道。70/30 是大量请求下的目标比例，不承诺每十次恰好七次和三次，也不保证 token 或费用比例。

三个通道设为同一个优先级时，三个模型共同分流；设为三个不同优先级时，形成按顺序使用的主备配置。

### 一次请求的执行过程

1. 校验调用方权限、请求格式、策略启用状态和请求级限流。
2. 在一致性读取中取得当前策略、绑定关系和通道配置，构成该请求的固定配置快照。
3. 排除停用、密钥不可用、冷却中、当前请求已尝试过、没有并发名额的通道；配置了预算时还需要价格可估算。
4. 从最高优先级的可用组中按权重选择通道，原子取得进程内全局和通道并发名额；竞争失败重新选择。全局名额已满则直接返回容量不足。
5. 检查该通道对应的精确结果缓存。命中则释放名额、记录缓存来源并返回；缓存故障按现有规则返回明确错误。
6. 未命中时先为本次尝试预留费用，再在剩余总时限内调用模型。未发送请求的失败必须释放预留和名额。
7. 得到有效输出后用策略阈值判定、结算费用、写缓存和请求记录，再返回结果。
8. 发生可切换错误时记录并结算本次尝试，释放名额，在同层剩余通道中重新选择。同层耗尽或没有容量时进入下一优先级。
9. 达到最大调用次数、总时限或无可用通道时结束，返回明确审核失败。

每个通道在一次请求中最多调用一次；最多调用次数包含第一次调用，建议默认 3 次，可配置 1～5 次。候选过滤和未发出请求的并发竞争不计入调用次数，但受总时限约束。

总调用时限建议初始 9000 ms，通道超时建议初始 4000 ms，实际单次时限为二者剩余时间的较小值。总时限可能提前终止切换，并不保证三次都能完成。迁移时保留原模型单次超时，避免无意改变已有调用行为。

费用结算与日志持久化另设有界收尾时间；接入方超时必须覆盖调用时限和收尾时间。当前文档建议的 sub2api 10000 ms 超时需要联动调整，初始可设为 15000 ms，并按实测延迟校准。内部切换开启后，sub2api 重试次数维持 0，避免重试层叠。

### 错误与恢复

| 情况 | 当前请求处理 | 通道状态处理 |
| --- | --- | --- |
| 网络错误、上游 5xx、单次超时 | 剩余时限允许则切换 | 连续 3 次此类失败后冷却 30 秒 |
| 上游 429 | 切换 | 遵守有效 Retry-After；缺失时冷却 30 秒 |
| 上游 401/403 | 切换 | 标记连接异常，修正密钥或手动验证恢复后重新参与 |
| 上游其他 4xx、模型不存在、参数不支持 | 切换其他独立通道 | 标记配置异常，修正后重新验证 |
| JSON 无效、缺字段、截断、模型拒答 | 切换 | 计入该通道失败统计 |
| 合法审核结果，包括低分或 flagged=true | 立即返回 | 清零连续临时失败计数 |
| 输入错误、无权限、调用方限流、预算耗尽、账务故障 | 直接返回错误 | 不归咎于模型健康，不绕过限制继续调用 |
| 调用方取消或总调用时限耗尽 | 停止后续调用，已有费用按实际情况结算 | 不计为通道自身故障 |

冷却结束后只放行一个真实请求试探，其他请求继续使用可用通道；成功恢复，失败重新冷却。第一期不启动定时付费探测。

服务端需保留上游 HTTP 状态、Retry-After、失败类别和是否发出调用，调度器据此判断；对外错误继续脱敏。现有实现把多个 HTTP 错误统一映射为 503，必须调整。

全部失败时返回 502/503/504 等明确错误，沿用接入方已有的失败放行或拦截设置。不会伪造 confidence=0 来表示调用失败，也不会因为某次审核命中而再换一个模型争取放行。

第一期面向现有单进程服务：健康状态、半开探测锁和并发计数保存在内存；进程重启后重新学习健康状态。多副本部署前需增加 Redis 或数据库租约协调全局容量、冷却和探测，不能把进程内限制宣称为集群限制。共享同一供应商账户的上游额度也不等同于各通道并发之和。

## 4. 页面与操作

侧栏调整为：运行概览、审核策略、审核模型、访问密钥、审核记录、成本与预算、系统设置。

“审核策略”保留策略选择和启停，下设三个页签：

- **审核规则**：提示词、判定阈值、缓存与记录设置。
- **模型调度**：通道表格，列为启用、通道名称、模型、优先级、权重、当前状态；底部设置总调用时限和最多调用次数。
- **审核试跑**：输入样本；选择“按调度规则”或指定已绑定通道，展示评分、结果、通道、切换过程、耗时和费用。

底部只有一个主按钮“保存并生效”。状态文案为“有未保存的修改”或“已保存 · 更新时间”。删除“保存草稿”“发布配置”“版本历史”“版本比较”“回滚”“线上 vN”“使用版本”等入口和展示。

试跑可直接提交当前编辑内容，不自动保存到正式策略；服务端仍完整校验配置和密钥绑定，记录为 test，使用同一调度实现，禁用正式结果缓存。指定通道试跑只调用该通道一次，结果中说明没有执行切换；费用计入试跑统计，仍受全局和通道并发限制。未保存内容仅存在当前编辑页。

启用策略至少要有一个结构有效、已启用且密钥有效的绑定通道。新建策略默认停用，配置完成后才能启用。手动停用最后一个可用通道时提示受影响策略；全部不可用的策略应明确展示“无可用模型”。

“审核模型”提供通道新增、编辑、启停、试跑和清除结果缓存，以及连接密钥管理。列表展示当前并发、近 24 小时调用次数、成功率、P95 耗时和健康状态；没有样本时显示“暂无数据”。引用中的通道不允许直接删除，先解除绑定或停用。

“审核记录”去掉策略版本列，增加实际通道、实际模型、尝试次数、缓存命中。详情按顺序展示每次调用及失败原因，根请求只计一次业务成功/失败，通道指标按各自尝试统计。

## 5. 当前配置的一致性

数据库只保存当前配置，不保存可查询、比较或恢复的历史配置。保存策略与其通道绑定在同一个事务中完成。

后台使用 ETag/If-Match 和内部并发控制值防止覆盖别人的修改，冲突返回 409。该值不在页面显示，不产生历史记录。共享通道更新也使用并发控制。

请求开始时以单条聚合查询或可重复读事务加载完整配置，避免新提示词配上旧通道列表。已经开始的请求继续使用自身快照；新请求使用保存后的配置。每次发起新尝试前再次检查通道、密钥和策略是否被停用，停用后不再发起新调用；已发送的请求可能完成。

新缓存键采用 HMAC，包含调用方、策略、当前规则与路由配置指纹、所选通道及连接配置指纹、密钥摘要、完整输入。任何审核规则、绑定关系或连接变化都会隔离旧缓存；旧缓存按 TTL 自然过期。

先按调度选定通道再读该通道缓存，不能用低优先级通道的旧结果覆盖当前选中的高优先级模型。后台新增“清除结果缓存”动作，以内部随机缓存代号实现失效，替代用户填写“连接修订”；上游网关自行修改模型映射时，本服务无法自动察觉，需要执行此动作。

## 6. 数据与接口调整

| 数据结构 | 变更 |
| --- | --- |
| audit_policies | 增加当前 config、内部并发控制值、updated_at；移除 draft、draft_revision、active_version 的业务用途 |
| audit_policy_versions | 迁移当前配置后删除；不再写入或查询历史版本 |
| audit_model_channels（新增） | 通道 id/name、provider、model、credential_id、timeout_ms、max_tokens、max_concurrency、enabled、内部并发控制值 |
| audit_policy_channels（新增） | policy_id/channel_id、priority、weight、enabled；策略与通道组合唯一 |
| audit_requests | 继续存根请求；增加最终通道、实际模型、尝试次数、配置指纹、汇总费用状态；不存完整历史提示词 |
| audit_attempts（新增） | attempt_id、request_id、序号、通道及模型快照、上游请求号、耗时、错误类别、用量和费用关联 |
| audit_costs / cost_adjustments | 从每根请求一笔升级为每次尝试一笔；核对记录绑定独立 cost_id，根 request_id 作为查询索引 |
| model_prices | 价格查询按供应商和模型区分，必要时增加连接范围，避免不同上游同名模型混价 |

管理员接口建议：

- `PUT /admin/policies/{id}/config`：原子保存规则、绑定列表和调度参数，保存后直接生效。
- `POST /admin/policies/{id}/test`：接收当前编辑配置、输入、按调度/指定通道模式，不保存配置。
- `GET/POST /admin/model-channels`、`PUT/DELETE /admin/model-channels/{id}`：通道管理。
- `POST /admin/model-channels/{id}/test`：验证单通道实际审核能力。
- `POST /admin/model-channels/{id}/cache/clear`：清除该通道结果缓存。
- `GET /admin/logs/{id}/attempts`：调用过程详情，仅管理员可见。

移除 `/draft`、`/publish`、`/rollback`、`/versions` 管理接口，旧入口返回明确的 410 或 404，前端同步移除调用。

### 对外协议兼容

已确认 sub2api 的 `backend/internal/service/content_moderation_custom.go` 强制要求 `schema_version=1` 且 `policy_version>=1`。直接删除字段或改为 0 会让所有自定义审核结果校验失败。

推荐分阶段处理：先删除本后台和后端的策略版本功能，兼容响应暂时保留正整数 `policy_version`，映射到自动生成的当前配置变更标识；没有历史列表或恢复能力。保留 schema_version=1，新增非敏感的实际模型、尝试次数等可选字段；请求根 model 仍是策略别名。

迁移时兼容标识从已有 active_version 起步，保存策略或修改它引用的通道时自动递增；停用策略的初始标识至少为 1，但只有有效启用策略才能正式调用。此标识只用于旧客户端兼容，不用它替代缓存中的完整配置指纹。

随后如需彻底删除对外 policy_version，先让 sub2api 接受无策略版本的新协议、去掉版本展示并补充兼容测试，再切换服务端响应，最后退役兼容字段。保留响应协议 schema_version，用于解析协议，与策略版本管理无关。

## 7. 费用与预算

现有 runAudit 只调用一次模型，audit_costs 以 request_id 为主键。加入切换后必须重构为“根请求 + 多次尝试 + 多笔费用”，不能只统计最后一次成功调用。

每次尝试都独立预留、结算、幂等落账；开始下一次尝试前重新检查调用方剩余预算。已发送但超时的调用可能仍被上游计费，保留待核对金额，不能当作免费或立即释放全部预算。

根请求费用汇总所有尝试。有任意一笔未知金额时，总额显示“待核对”，同时显示已知小计，避免把小计误当完整费用。费用结算或记录失败停止后续切换，避免进一步扩大无法记账的调用。

RPM 按外部根请求计算一次；通道请求数、token 和费用按实际尝试分别计算。缓存命中记根请求和零新增上游成本，不增加模型实际调用次数。

当前 Grok 调用无法取得可靠的逐请求货币成本：保持“待核对”，不套用 DeepSeek 单价。配置人民币预算的调用方自动排除无可靠预估价格的通道；全部通道被排除时返回 pricing_unavailable，页面需显示“该调用方预算设置下不可用”。不设人民币预算时才允许使用未知价格通道，并保留现有费用核对机制。

## 8. 迁移和实现顺序

1. 新增迁移脚本与数据结构，在事务中将各策略当前已发布配置迁为当前 config；绝不让尚未发布的草稿覆盖线上配置。
2. 从当前配置生成模型通道和一条绑定，默认优先级 1、权重 100，保持旧服务行为。无已发布配置的策略迁入原草稿内容但保持停用，等待完成配置后保存启用。
3. 已发布策略的未发布修改不自动导入新当前配置；迁移报告列出这类策略数量和名称。迁移前备份包含这些内容，系统不再提供草稿入口。
4. 旧费用逐笔映射到独立 cost_id 并迁移核对外键，保持金额和预算汇总一致；旧单次真实调用映射为一条尝试，旧缓存命中不伪造成上游调用。无法从历史记录还原的字段标为未知，不猜测。
5. 改造执行链为固定配置读取、调度、逐次调用、逐次计费、根请求汇总；继续复用 DeepSeek Chat Completions 和 Grok Responses/SSE 适配器。
6. 替换后台配置、试跑、日志和模型通道页面，删除版本功能，更新 README、部署和 Grok 配置说明。
7. 验证迁移后删除历史版本表及旧配置字段；保留迁移前数据库备份，运行时不依赖旧表。旧账单、操作记录、请求记录按各自原保留规则处理，不因删版本功能而丢失。
8. 使用维护窗口切换数据库和服务，避免新旧二进制混跑。需要退回旧程序时恢复配套备份，并处理切换后的新增账务记录；不能仅替换旧二进制。

实现主要涉及 `model.go`、`schema.sql`、`store.go`、`server.go`、`engine.go`、`provider.go`、`cache.go`、`billing.go`、前端 `App.vue`/`api.ts`/`BillingPanel.vue`。新增 `router.go`，并将模型通道、调度表单和试跑视图拆成独立 Vue 组件。

## 9. 验收重点

- 页面不再出现草稿、发布、历史版本、比较和回滚；保存直接影响新请求，未保存试跑不改线上配置。
- 两个管理员同时保存时冲突可见；并发请求不会混用不同保存操作中的规则和通道列表。
- 同优先级按权重分流，低优先级只在高优先级无可用候选时调用；使用可注入随机源验证选取边界，避免易波动的概率测试。
- 覆盖失败切换、同层耗尽、并发争抢、半开单探测、Retry-After、取消、总超时和次数上限；有效违规结果不会触发切换。
- 逐次预留和结算覆盖“失败后成功”“超时金额未知”“预算不足”“结算失败”“幂等重试”，根请求总费用和账单一致。
- 规则、权重、连接或密钥变化后不命中旧缓存，不同模型缓存互相隔离；命中缓存不伪造新增上游用量。
- 从同时含已发布配置和未发布草稿的旧库迁移，确认当前策略、旧账单和核对外键均正确。
- 现有 sub2api 校验器能够继续解析响应；DeepSeek 与 Grok 测试覆盖不同接口和错误状态，受控 HTTP 替身完成验证，不调用付费模型。

第一期交付范围：移除版本功能、当前配置直接保存、多模型通道、优先级加权分流、有限故障切换、通道并发与冷却、调用过程日志、费用与缓存适配。投票审核、并行竞速、自动按价格选模型和集群调度留待实际需要时再扩展。
