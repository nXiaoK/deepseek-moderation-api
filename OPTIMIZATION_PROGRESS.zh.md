# 优化实施记录

基线：97edb43。保持保存即生效、单实例部署和现有审核协议；不调用付费模型验证，不自动推送或部署生产。

| 批次 | 交付范围 | 状态 |
| --- | --- | --- |
| 1 | 提示与评测说明、日志检索和批量费用汇总、静态资源缓存 | 已完成：0884f8c、5d2a086、4c51629 |
| 2 | 缓存与模型容量解耦、入口资源限制、试跑容量隔离、重复请求合并 | 已完成：7aef6e6、4d3fb92 |
| 3 | 连接价格覆盖、分析维度、图文试跑、样本与批量评测、输出留存 | 已完成；评测后端、工作流、导出与桌面手机检查通过 |
| 4 | 运维诊断与告警、CI、迁移记录和恢复验证、管理流程补齐 | 已完成，完整回归及本地服务验证通过 |

多副本、MFA/角色、复杂投票调度与多币种属于需求触发项，本次不提前引入。

第三批已提交：

- 1037863：连接价格覆盖、恢复默认及历史价格快照。
- 5ac0ddd：图文试跑、上传与范围留痕。
- 20a3135：模型原始输出独立加密与保留期。
- 813a67c：分析维度、调用结果与分位耗时、请求统计及日志联动。
- 2c42430：加密样本库与受预算约束的批量评测工作台。

样本库与批量评测已完成：加密样本、内置 48 条样本导入、配置快照、多通道/重复评测、费用上限与取消、重启中断恢复、误报/漏判/失败分开统计、评测结果导出。测试只使用受控替身，不运行真实模型评测。

第四批按以下边界实施：站内运行告警和受保护的诊断数据；固定 CI 检查与迁移版本记录；隔离恢复演练；调用方密钥编辑/过期/原身份轮换；策略归档和受引用保护的删除、配置导入导出；日志 CSV 与已保存文本加入评测样本。不会发送外部通知，不引入多副本或角色系统。

第四批提交：

- 0ed4164：带校验值与兼容性检查的顺序迁移。
- 23a8741：CI 工作流与实际 pg_dump/pg_restore 测试。
- 8b695be：访问密钥编辑、到期、访问记录与保持身份的轮换。
- 156a34a：策略归档/恢复/删除/导入导出、变更摘要。
- 2f131ba：运行状态、站内告警、通道验证状态与会话隔离。
- 本记录所在收尾提交：审核摘要 CSV、人工标注样本衔接、持久化失败告警与评测超时收尾。

## 最终验证

- 完整 `go test -race ./... -count=1` 通过，启用了真实 PostgreSQL 集成和隔离恢复演练；audit 包约 117 秒。恢复演练只使用测试 schema 与随机临时测试数据库。
- 25 项 Playwright 全部通过，包含 1440、768、390、320 像素布局、图表交互、密钥与策略生命周期、图文试跑、评测、CSV、会话过期与迟到响应。
- 前端类型检查/生产构建、Prettier、Git diff 空白检查通过。
- 本机开发服务已重启，使用原有本机数据库 `audit_multimodel_local`；迁移版本 1、2、3 已应用。
- 实际管理员登录与 9 个只读页面检查通过，浏览器运行错误为 0。本地入口为 `http://localhost:5173`。
- 未调用真实付费模型进行评测，未推送远程，未部署生产。CI 工作流已配置，远程 CI 尚未触发。
- 老的明文模型输出按原保留期清理，未自动改写；连接级价格启用后的旧版本回退限制见 UPGRADE.zh.md。
