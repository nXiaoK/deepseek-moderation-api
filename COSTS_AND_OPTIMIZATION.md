# 成本统计、预算与 token 优化

实现范围：上游模型成本统计（含官方、第三方与 sub2api 通道）、每个调用方访问密钥的日/月预算、可选精确结果缓存。这里记录系统的供应商成本，不向调用方充值或扣除用户余额。

## 当前单价

2026-09-12 查阅 [DeepSeek 官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/) 后初始化。单位为人民币 / 百万 tokens。

| 模型 | 时段 | 缓存命中输入 | 未命中输入 | 输出 |
| --- | --- | --- | --- | --- |
| deepseek-flash | 空闲 | 0.02 | 1 | 4 |
| deepseek-flash | 高峰 | 0.04 | 2 | 8 |
| deepseek-v4-pro | 空闲 | 0.15 | 4.5 | 13.5 |
| deepseek-v4-pro | 高峰 | 0.30 | 9 | 27 |

高峰为北京时间周一至周五 09:00–12:00、14:00–18:00，其余为空闲；没有擅自加入节假日例外。历史 Flash 别名 `deepseek-v4-flash`、`deepseek-v4-flash-vision-exp` 按官方说明映射至当前 Flash 价目。

后台“成本与预算 → 模型单价”按“审核模型”通道配置的模型名匹配，区分大小写；例如通道模型名为 `gpt-5.6-luna`，单价的模型名称也填 `gpt-5.6-luna`。计价范围可选“模型默认”或一个连接密钥：优先使用连接覆盖，没有覆盖时使用模型默认价格。通道显示名称与上游响应的模型名不参与选价。相同模型在不同供应商连接可配置不同单价；没有分时价格时，高峰和空闲填写相同单价。恢复模型默认价格只影响后续请求，已经预留及历史费用仍使用原价格快照。

每个成本记录保存请求当时的价格快照，保存或修改单价只影响之后的新请求，不会补算历史待核对记录。未知模型不会自动套用 Flash 价格；启用了预算的调用方必须先配置该模型单价。

## 成本如何计算

```text
成本 = (缓存命中输入 tokens × 命中单价
      + 缓存未命中输入 tokens × 未命中单价
      + 输出 tokens × 输出单价) / 1,000,000
```

使用 DeepSeek 实际返回的 `prompt_cache_hit_tokens`、`prompt_cache_miss_tokens`、`completion_tokens`。推理 tokens 是输出中的子集，不额外重复计费。金额以整数 pico-CNY（1 元 = 10^12 单位）存储，不逐请求四舍五入到分；对外金额用十进制字符串。

示例：Flash 空闲时段，6000 命中输入 + 1000 未命中输入 + 40 输出，成本为 0.00128 元。这里是给定用量的计算示例，不是本项目已测得的真实流量。相同用量全部输入未命中为 0.00716 元，约少 82.1% 的计算费用；实际节省取决于真实缓存命中情况。

系统保留以下状态，避免把未知费用当作免费：

| 状态 | 语义 |
| --- | --- |
| 用量计价 | 根据上游返回用量与本地价格快照计算，不是已核对的官方实扣账单 |
| 保守估算 | 没有完整缓存拆分时按未命中估算；请求跨计费时段时按较高单价估算 |
| 预留中 | 请求正在执行，预留资金占用预算 |
| 待核对 | 请求已发出但用量缺失/异常、计价缺失或进程中断；保留预留占用 |
| 已核对 | 管理员根据供应商记录填写金额与依据；保留核对前状态和操作人 |
| 本地缓存 | 未调用 DeepSeek，新增上游费用和 token 都为零 |
| 无上游调用 | 模型调用之前已因容量或参数等原因失败，无新增模型费用 |

按本系统请求开始时间选择时段。官方文档没有明确跨时段请求的最终归属细则，因此跨边界会标为估算。网络超时不能证明供应商未计费。没有可靠用量时，后台可依据供应商账单将费用核对为零或实际金额。

审核结果 JSON 格式错误但已返回可信 usage 的请求仍可能产生模型费用，照常记录。历史审核日志没有所需计量字段时不伪造精确费用；新成本账本从启用本模块后开始记录，并独立于输入日志的保留期。目前成本记录不自动删除。

## 预算

在“成本与预算 → 调用方预算”设置每日和每月预算，留空表示不限，0 表示没有新增上游调用预算。周期以北京时间自然日和自然月计算，归属请求开始时的周期。管理员试跑单独记成本，不占某个正式调用方的预算。

调用前根据提示词、输入的 UTF-8 字节量、消息包装余量、最大输出 tokens 和较高时段价格做保守预留。该估算不冒充官方 tokenizer。数据库对调用方加行锁，检查“已计量 + 未结算预留 + 本次预留”后才允许发起新调用。

完成后结算；有可信完整计量就释放多余预留。未知用量继续保留，后台核对后调整。实际计量超过预留时如实记录，后续调用继续受额度限制，所以这是基于保守预估的支出控制，不是 DeepSeek 账户层面绝对不可超出的硬限额。

预算不足返回 HTTP 429 `budget_exceeded`。这阻止新的审核模型支出；sub2api 是否继续处理业务请求由其审核失败策略决定。缓存命中不增加上游费用，因此额度耗尽时仍可使用有效的本地缓存。

当前“调用方”以访问密钥为单位，多个密钥尚不共享同一个账户预算。月预算占用达到 80% 时界面突出显示；尚未接入邮件等外部提醒。

## token 优化

已保留：非思考模式、短 JSON 输出、重试默认关闭、固定 system 提示词放在前面。

新增：策略“审核规则”页提供结果缓存时间，0 为关闭、最多 3600 秒，修改后点击“保存并生效”。缓存键通过 HMAC 生成，包含调用方、当前策略与调度配置、模型通道、实际凭证摘要和完整文本；密钥轮换、配置保存或输入变化都会失效。只缓存成功审核结果，后台试跑始终调用真实模型。缓存默认关闭，原提示词不变。

DeepSeek 的 [前缀缓存](https://api-docs.deepseek.com/zh-cn/guides/kv_cache) 默认自动开启，相同前缀需要先落盘，不保证每次命中。不要在固定提示词前插入时间戳、随机 ID 或用户信息。前缀缓存降低重复输入单价，本地结果缓存直接避免一次模型调用，后台分别展示两种命中。

建议后续改进，按收益和依赖排序：

1. 建立人工标注评测集，比较误报、漏报、JSON 失败率、延迟和每千次成本。
2. 在保留原版的基础上另存“精简模板”做对照评测，清理重复规则和转义垃圾；验证通过后由管理员保存生效。
3. 把输出上限从 512 调到 128～256 做测试。`max_tokens` 是上限，降低它不代表按比例省钱；已有回答很短时费用可能不变，过低可能截断 JSON。
4. 合并同时到达的相同请求，只共享一次模型评估；需处理等待者取消和独立预算归属。
5. 增加误判标注、样本回放和模型对照测试；避免仅靠置信度调阈值。
6. 增加调用方账户、多密钥共享预算、统一总预算、日成本趋势、CSV 导出和预算提醒。
7. 给异步样本评测提供空闲时段运行选项；同步拦截不为省时段价格而延后业务请求。

不建议用另一台大模型先总结再审核，或静默截断输入：会增加一次调用，并可能丢失审核所需证据。也不建议通过模糊语义相似度直接复用“通过”结果；本地缓存只对完整输入一致的请求生效。

## 多模型尝试费用

每次上游尝试有独立费用 ID，关联同一个外部请求 ID。成功前的失败、超时也可能计费；根请求合计有未知项时显示“待核对”及已知费用小计。开始下一次调用前重新检查预算；账务异常停止继续切换。RPM 按根请求计算一次。

所有通道均按配置模型名查找价格，并按上游报告用量计价；无单价或没有可信用量时保持待核对。人民币预算下允许有单价的文本通道，排除无单价模型与无法预估的图片请求，无可估价通道则返回 pricing_unavailable。费用核对按独立费用 ID 操作，账单不会因策略删除版本功能而被忽略。
